import { NewUppercasePipe } from './new-uppercase.pipe';

describe('NewUppercasePipe', () => {
  it('create an instance', () => {
    const pipe = new NewUppercasePipe();
    expect(pipe).toBeTruthy();
  });
});
